# ============================================================
# BanVic ELT - variáveis de ambiente
# Copie para .env e preencha os valores reais.
# NUNCA versione o .env.
# ============================================================

# -- Source Postgres (ERP simulado) ---------------------------
SOURCE_POSTGRES_USER=banvic
SOURCE_POSTGRES_PASSWORD=changeme_source
SOURCE_POSTGRES_DB=banvic
SOURCE_POSTGRES_HOST=source-postgres
SOURCE_POSTGRES_PORT=5432

# -- DW Postgres (analytics_dw + metabase_app) ----------------
DW_POSTGRES_USER=analytics
DW_POSTGRES_PASSWORD=changeme_dw
DW_POSTGRES_DB=analytics_dw
DW_POSTGRES_HOST=dw-postgres
DW_POSTGRES_PORT=5432

# -- Airflow metadata DB ---------------------------------------
AIRFLOW_DB_USER=airflow
AIRFLOW_DB_PASSWORD=changeme_airflow
AIRFLOW_DB_NAME=airflow

# -- Airflow internals -----------------------------------------
# Gere com: python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
AIRFLOW_FERNET_KEY=CHANGE_ME_generate_a_real_fernet_key_here
AIRFLOW__WEBSERVER__SECRET_KEY=changeme_webserver_secret
AIRFLOW_ADMIN_PASSWORD=changeme_admin

# -- Metabase --------------------------------------------------
MB_DB_TYPE=postgres
MB_DB_DBNAME=metabase_app
MB_DB_HOST=dw-postgres
MB_DB_PORT=5432

# -- dbt (usa DW_POSTGRES_PASSWORD) ---------------------------
DBT_PASSWORD=changeme_dw