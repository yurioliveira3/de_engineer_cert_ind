banvic_dw:
  target: dev
  outputs:
    dev:
      type: postgres
      host: "{{ env_var('DW_POSTGRES_HOST', 'dw-postgres') }}"
      user: "{{ env_var('DW_POSTGRES_USER', 'analytics') }}"
      password: "{{ env_var('DBT_PASSWORD') }}"
      port: 5432
      dbname: analytics_dw
      schema: staging
      threads: 4
